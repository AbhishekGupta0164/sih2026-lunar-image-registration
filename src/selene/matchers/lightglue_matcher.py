"""ALIKED/SuperPoint + LightGlue. Default expert for same-modality, similar-sun (|delta az| < 40 deg), moderate scale-ratio tiles. Runs on CPU for small tiles, GPU optional for larger ones.

Owner: P3
"""
