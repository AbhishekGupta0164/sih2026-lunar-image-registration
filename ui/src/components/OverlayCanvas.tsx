// Owner: P4 — draws match lines (colour-coded by `method`), residual
// arrows, and the checkerboard/wipe comparison over the registered raster.
import React from "react";

export default function OverlayCanvas() {
  return <canvas />;
}
